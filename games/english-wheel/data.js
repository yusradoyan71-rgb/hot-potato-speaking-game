/**
 * ENGLISH WHEEL - Data alias for expressions dataset
 */
if (typeof grade7Expressions === "undefined" && typeof module !== "undefined") {
  const exp = require("./expressions.js");
  module.exports = exp;
}
